import React from 'react';

// This component is no longer used as we switched to native Telegram Authentication.
// Kept as a placeholder to prevent import errors if referenced elsewhere.
const Auth: React.FC = () => {
  return null;
};

export default Auth;